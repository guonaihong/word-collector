# Agent Rules

## 禁止写兜底逻辑

不要写防御性的兜底/fallback 代码。具体包括：

- **不要为不可能发生的场景写 fallback 分支。** 如果某个值理论上不会为空，就不要加 `if x == nil` 或 `if len(x) == 0` 的兜底返回。
- **不要为了"以防万一"加 try-catch 或 recover。** 只有在调用方确实需要处理错误时才处理，不要为了"安全"而吞掉 panic 或 error。
- **不要给函数加多余的默认值。** 如果调用方有义务传正确的参数，就不要在函数内部再设一套默认值。
- **不要写"保底"返回值。** 比如 `return result, nil` 中的 result 不应该是零值兜底，而应该是正确计算出来的值。如果计算失败，直接返回 error。

**原则：让问题暴露在明处，而不是被兜底逻辑悄悄掩盖。** 出了 bug 应该立刻崩溃或报错，而不是静默返回一个无意义的默认值。
